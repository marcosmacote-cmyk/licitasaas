{
  "name": "licitasaas-local-watcher",
  "version": "1.0.0",
  "description": "ComprasNet Chat Watcher — roda localmente e envia mensagens para o LicitaSaaS",
  "main": "watcher.js",
  "scripts": {
    "start": "node watcher.js",
    "setup": "npm install && npx playwright install chromium"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2",
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "body-parser": "^2.2.2",
    "express": "^5.2.1"
  }
}
